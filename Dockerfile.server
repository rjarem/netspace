FROM node:22-alpine
WORKDIR /app
RUN npm install -g pnpm@12
# Copy whole workspace context (build at repo root)
COPY . .
RUN pnpm install --frozen-lockfile --ignore-scripts \
 && cd packages/server && pnpm exec tsc
WORKDIR /app/packages/server
EXPOSE 2567
CMD ["node", "dist/index.js"]