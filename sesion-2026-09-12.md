# GrooveRadius (NetSpace) — Reporte de Sesión 5 — 2026-09-12

## Estado al cierre: FUNCIONANDO EN PRODUCCIÓN ✅
URL: https://play.turedvirtual.vip · API: https://api.turedvirtual.vip/api/health
Version bundle: `v23-sync-poll` (el banner aparece inmediatamente al conectar)

## Logros del día

1. **Teletransporte por píldora (9566db3 + f0e3ad4)** — tap en el nombre de un usuario te
   transporta al tile caminable más cercano A SU LADO (no encima), cámara te sigue, status
   🚀 nombre. Verificado end-to-end con probes ANTES de deploy.
2. **Caché de deploys resuelto a nivel servidor** — `serve` reemplazado por nginx:
   - index.html → Cache-Control: no-store (cada visita trae la versión actual)
   - /assets/*.js → immutable (hash estable = cache eterno seguro)
   - Desde hoy, TODO deploy llega a todos los dispositivos con una recarga normal.
3. **Bug MIME (regresión #2)** — nginx.conf generado a mano sin `include mime.types` servía
   JS como text/plain → navegador rechaza módulos. Fix + checklist en bitácora.
4. **Banner de versión visible** (2a80faf) — solo aparecía si LiveKit conectaba; ahora se
   pinta al conectar. Clave para diagnosticar qué bundle corre cada dispositivo.
5. **Roster reconcile (a4b3bd2) — EL FIX DEL DÍA** — joiners eran invisibles para quien ya
   estaba dentro porque room.state.players.onAdd (evento Colyseus) es poco confiable.
   Ahora cada 250ms el cliente concilia su roster contra el estado del server: agrega
   ausentes, elimina fantasmas. La pantalla SIEMPRE converge a la verdad del server.
   LEYENDA: no depender de eventos de schema de Colyseus para NADA (posiciones ya, roster
   ahora). Poll de reconciliación = fuente de verdad.
6. **Refactor de main.ts** — 901 líneas → módulos ≤400 (ver sección pendientes).

## Infra

- Deploy vía Dokploy (composeId 2oSGoBF8WnTHutT5rIyGp, panel.turedvirtual.vip)
- Cada deploy: git push → update compose (SHA nuevo) → compose.deploy → verificar
  bundle hash + content-type + health. Restart de contenedores mata tabs del navegador
  del agente (recorrerlas tras cada deploy).
- Disco LLM-PC: crash-loop del gateway (systemd vs gateway manual --replace) inflaba
  gateway-exit-diag.log (250MB, 377k eventos). Log truncado, 18GB libres. PENDIENTE:
  dejar UN solo runner del gateway (restart con visto de Tito) + limpieza opcional 10GB
  (npm cache 5GB, backups viejos 5.5GB, skills duplicados en perfiles 3.4GB).

## Bugs/lecciones nuevos en bitácora (netspace-bugs-y-soluciones-sesion3.md)
- Bug 19: píldora = teletransporte (no vuelo de cámara)
- Bug 20: caché → nginx no-store (la clase de bug "este dispositivo no se actualizó" muere)
- Bug 21: MIME text/plain (2a vez — checklist nginx: mime.types + default_type + root)
- Bug 22: repro end-to-end de sync con 3 probes
- Bug 23: roster reconcile (joiners invisibles)

## Ideas futuras documentadas (propuestas de la esposa de Tito)
- Salón de clases: sillas asignadas + transmisión global (presentador/video) visible
  desde cualquier punto, networking después. → ideas-futuras-escenarios.md
- Control de micrófono: mute propio (usuario) + mute forzado (admin, anti-feedback).
  → ideas-futuras-escenarios.md

## Pendientes (prioridad)
1. Restart gateway en LLM-PC (un solo runner) — con visto de Tito
2. Limpieza disco opcional ~10GB
3. Verificar refactor de main.ts con probes ANTES de deploy
4. Panel admin de zonas (embed interactivo, stream HLS, lista de acceso, baneo)
5. Interfaz/UX: refinar visual (comentario de Tito al cierre)
6. Avatar en Brave no funciona (Chrome sí) — diagnosticar Shields/caching
7. Foto-avatar obligatoria al entrar, fondo full-app
8. Comprar/apuntar grooveradius.vip + rebranding UI
